name: CD github runner

on:
  push:
    branches: 
      - main
  pull_request:
    branches: 
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull docker image
        run: docker pull paulamariapires/user:latest

      
